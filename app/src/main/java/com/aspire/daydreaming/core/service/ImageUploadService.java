package com.aspire.daydreaming.core.service;import android.app.Service;import android.content.Intent;import android.os.Binder;import android.os.IBinder;import android.support.annotation.Nullable;/** * @Name: ImageUploadService * @Author: yeliang * @Date: 2016/9/14 */public class ImageUploadService extends Service {    private ImageUploadBinder mBinder;    @Nullable    @Override    public IBinder onBind(Intent intent) {        return mBinder;    }    @Override    public void onCreate() {        super.onCreate();        mBinder = new ImageUploadBinder();    }    @Override    public void onStart(Intent intent, int startId) {        super.onStart(intent, startId);    }    @Override    public void onDestroy() {        super.onDestroy();    }    public class ImageUploadBinder extends Binder {        public ImageUploadService getService() {            return ImageUploadService.this;        }        // add method here    }}